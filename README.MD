# 🐙 CLIGRAM: Консольный Клиент Telegram

## Описание
CLIGRAM — консольный клиент для Telegram, построенный на библиотеке Telethon. Он предназначен для автоматизации задач и быстрого взаимодействия с мессенджером через командную строку.

## 🚀 Технологии

Проект создан с использованием:
* **Python** (3.8+)
* **Telethon** — для взаимодействия с Telegram API.
* **Pillow** — для вывода картинок из чата в ASCII


## ⚙️ Установка

Чтобы начать работу, выполните следующие шаги:

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <https://github.com/adolfgay/CLIgram.git>
    cd cligram
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python3 -m venv .venv
    source .venv/bin/activate  # Для Linux/macOS
    # .venv\Scripts\Activate.ps1  # Для Windows PowerShell
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

## 🔑 Настройка (.env файл)

Для работы клиента необходимо получить `api_id` и `api_hash` на [сайте Telegram API](https://my.telegram.org/).

Создайте в корне проекта файл `.env` и добавьте в него ваши ключи:

```dotenv
# .env файл
API_ID=1234567
API_HASH=abcdef123456abcdef123456abcdef123456
```

## ▶️ Запуск

После настройки окружения и файла .env вы готовы запустить клиент:

Убедитесь, что ваше виртуальное окружение активно.

Запустите основной файл клиента:

```bash
python main.py
```

Первый запуск:

При первом запуске клиент Telethon запросит у вас номер телефона вашего аккаунта и код подтверждения (придет в Telegram или по SMS).(нет мне это не отправляется)

Если у вас включена двухфакторная аутентификация (2FA), клиент также запросит пароль.

После успешного входа будет создан файл сессии (bot.session), который позволит вам входить автоматически в дальнейшем.